- ES6箭头函数和ES5普通函数一样吗
不一样的，普通函数绑定的是运行时this, 箭头函数绑定的创建时的this === this.xxx.bind(this)
- this的作用
    对象方法调用：指向对象  
    函数调用：指向全局对象，即使在函数内部定义指向依旧是全局对象  
    构造函数调用：指向新构造的对象  
    apply, call, bind均是用来绑定this, 但是在箭头函数中不起作用  